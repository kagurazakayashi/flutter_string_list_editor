## 1.0.0

- 首次发布：从 MyTongdy 抽取的可视化字串列表编辑器。
- 提供 `StringListEditor` widget、`showStringListEditorDialog` 与
  `showDelimitedTextEditorDialog` 对话框函数。
- 提供 `Delimiter` 分隔符与 `splitList` / `joinList` 工具函数。
- 内置英文、简体中文、繁体中文三套本地化。
